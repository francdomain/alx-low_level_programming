#include "main.h"
#include <stdio.h>

/**
  * main - function that prints its name
  * @argc: argument count
  * @argv: argument array of commands listed
  *
  * Return: Always zero
  */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
